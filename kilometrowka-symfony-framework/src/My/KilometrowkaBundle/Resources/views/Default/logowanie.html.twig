{% extends 'MyKilometrowkaBundle:Default:layout.html.twig' %}


{% block body %}
    {#<%@page contentType="text/html" pageEncoding="UTF-8"%>
    <%@ taglib prefix="form" uri="http://www.springframework.org/tags/form"%>   
    <%@taglib uri="http://www.springframework.org/tags" prefix="spring"%>
    <%@ taglib uri='http://java.sun.com/jsp/jstl/core' prefix='c'%>#}

    {% if user is defined  %}
        {{ user[0].login }}
    {% endif %}
    <center>
        {% if logowanie is defined  %}
            {% if logowanie==1 %}
                <div class="alert alert-dismissable alert-danger">
                    <button type="button" class="close" data-dismiss="alert">&times;</button>
                    Dane logowania nieprawidłowe. Spróbuj ponownie!
                </div>
            {% endif %}
        {% endif %}

        {% if app.request.query.get("blad")==1 %} 
            <div class="alert alert-dismissable alert-success">
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                Rejestracja przebiegła pomyślnie!
            </div>
        {% endif %}

        {# <c:if test="${logowanie==1}">
             <div class="alert alert-dismissable alert-danger">
                 <button type="button" class="close" data-dismiss="alert">&times;</button>
                 Dane logowania nieprawidłowe. Spróbuj ponownie!
             </div>
         </c:if>#}

        {% if rejestracja is defined  %}
            {% if rejestracja==1 %}
                <div class="alert alert-dismissable alert-success">
                    <button type="button" class="close" data-dismiss="alert">&times;</button>
                    Rejestracja przebiegła pomyślnie. Teraz możesz się zalogować.
                </div>
            {% endif %}
        {% endif %}
        {# <c:if test="${reg==1}">
             <div class="alert alert-dismissable alert-success">
                 <button type="button" class="close" data-dismiss="alert">&times;</button>
                 Rejestracja przebiegła pomyślnie. Teraz możesz się zalogować.
             </div>
         </c:if>#}
        <form action="logowanie" method="post" commandName="dodajLogowanieForm">
            <table border="0">

                <tr>

                    <td><label class="form-label">Login:</label><input path="login" class="form-control" name="login"/><font color="red"><form:errors path="login" /></font></td>
                </tr>
                <tr>

                    <td><label class="form-label">Hasło:</label><input path="haslo" class="form-control" type="password" name="haslo"/><font color="red"><form:errors path="haslo" /></font></td>
                </tr>

                <tr>
                    <td colspan="2" align="center"><br><input type="submit" value="Loguj" class="btn btn-primary btn-sm"/></td>
                </tr>
            </table>
        </form>
    </center>
{% endblock %}